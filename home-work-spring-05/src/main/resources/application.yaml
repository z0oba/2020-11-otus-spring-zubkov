spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    schema: schema.sql
    data: data.sql
  shell:
    interactive:
      enabled: true
  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true